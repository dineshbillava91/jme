$(function () {
    
    $("#BannerBanner").change(function () {
        $(".video-thumbnail").html("");
        var regex = /^([a-zA-Z0-9\s_\\.\-:])+(.jpg|.jpeg|.gif|.png|.bmp)$/;
        if (regex.test($(this).val().toLowerCase())) {
            if ($.browser.msie && parseFloat(jQuery.browser.version) <= 9.0) {
                $(".video-thumbnail").show();
                $(".video-thumbnail")[0].filters.item("DXImageTransform.Microsoft.AlphaImageLoader").src = $(this).val();
            }
            else {
                if (typeof (FileReader) != "undefined") {
                    $(".video-thumbnail").show();
                    $(".video-thumbnail").append("<img />");
                    var reader = new FileReader();
                    reader.onload = function (e) {
                        $(".video-thumbnail img").attr("src", e.target.result);
                    }
                    reader.readAsDataURL($(this)[0].files[0]);
                } else {
                    alert("This browser does not support FileReader.");
                }
            }
        } else {
            alert("Please upload a valid image file.");
        }
    });
    
    $("#VideoVideoThumbnail").change(function () {
        $(".video-thumbnail").html("");
        var regex = /^([a-zA-Z0-9\s_\\.\-:])+(.jpg|.jpeg|.gif|.png|.bmp)$/;
        if (regex.test($(this).val().toLowerCase())) {
            if ($.browser.msie && parseFloat(jQuery.browser.version) <= 9.0) {
                $(".video-thumbnail").show();
                $(".video-thumbnail")[0].filters.item("DXImageTransform.Microsoft.AlphaImageLoader").src = $(this).val();
            }
            else {
                if (typeof (FileReader) != "undefined") {
                    $(".video-thumbnail").show();
                    $(".video-thumbnail").append("<img />");
                    var reader = new FileReader();
                    reader.onload = function (e) {
                        $(".video-thumbnail img").attr("src", e.target.result);
                    }
                    reader.readAsDataURL($(this)[0].files[0]);
                } else {
                    alert("This browser does not support FileReader.");
                }
            }
        } else {
            alert("Please upload a valid image file.");
        }
    });

});
